use world;
use jugos;
create table tbclientes3(dni varchar(20), nombre varchar(150), direccion1 varchar(150), direccion2 varchar(150), barrio varchar(50), ciudad varchar(50),
estado varchar(50), cp varchar(10), edad smallint, sexo varchar(1), limite_credito float, volumen_compra float, primera_compra bit(1));
CREATE TABLE TABLA_DE_VENDEDORES (
    MATRICULA varchar(5), 
    NOMBRE varchar(100), 
    PORCENTAJE_COMISION float);
    
